import { Meta, StoryObj } from '@storybook/react';
import { Header } from './';
export default {
    title: 'Components/SideBar',
    component: Header,
} as Meta;

export const Default: StoryObj = {};
